<Page x:Class="BankShibaevaAnna322.CreditsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Управление кредитами">
    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Page.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Управление кредитами" Grid.Row="0" Style="{StaticResource HeaderTextStyle}" Margin="0,0,0,20"/>

        <WrapPanel Grid.Row="1" HorizontalAlignment="Center" Margin="0,0,0,20">
            <ComboBox x:Name="ClientFilterComboBox" Width="200" Margin="0,0,10,0" DisplayMemberPath="FullName"/>
            <ComboBox x:Name="StatusFilterComboBox" Width="150" Margin="0,0,10,0">
                <ComboBoxItem Content="Все" IsSelected="True"/>
                <ComboBoxItem Content="Активен"/>
                <ComboBoxItem Content="Погашен"/>
                <ComboBoxItem Content="Просрочен"/>
            </ComboBox>
            <Button Content="Фильтр" Width="80" Click="FilterButton_Click" Style="{StaticResource CustomButtonStyle}"/>
            <Button Content="Сброс" Width="80" Margin="10,0,0,0" Click="ResetButton_Click" Style="{StaticResource CustomButtonStyle}"/>
        </WrapPanel>

        <DataGrid x:Name="CreditsDataGrid" Grid.Row="2" AutoGenerateColumns="False" IsReadOnly="True" Margin="0,0,0,20">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Клиент" Binding="{Binding ClientFullName}" Width="*"/>
                <DataGridTextColumn Header="Тип кредита" Binding="{Binding LoanType}" Width="*"/>
                <DataGridTextColumn Header="Сумма" Binding="{Binding Amount, StringFormat={}{0:C}}" Width="*"/>
                <DataGridTextColumn Header="Ставка" Binding="{Binding InterestRate, StringFormat={}{0}%}" Width="*"/>
                <DataGridTextColumn Header="Срок" Binding="{Binding Duration, StringFormat={}{0} мес.}" Width="*"/>
                <DataGridTextColumn Header="Дата начала" Binding="{Binding StartDate, StringFormat={}{0:dd.MM.yyyy}}" Width="*"/>
                <DataGridTextColumn Header="Статус" Binding="{Binding Status}" Width="*"/>
                <DataGridTemplateColumn Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Редактировать" Click="EditButton_Click" Style="{StaticResource CustomButtonStyle}" Margin="2"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Добавить кредит" Width="150" Margin="5" Click="AddButton_Click" Style="{StaticResource CustomButtonStyle}"/>
            <Button Content="Удалить кредит" Width="150" Margin="5" Click="DeleteButton_Click" Style="{StaticResource CustomButtonStyle}"/>
        </StackPanel>
    </Grid>
</Page>